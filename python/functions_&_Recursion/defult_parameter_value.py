# Defult parameter value
def greet(name="stranger"):
    print("good day, "+ name)

greet("rutik")
greet()
# We can have a value as defult argument in a function
# If we specify name="stranger" in the line containing def
# this value is used when no argument is passed
