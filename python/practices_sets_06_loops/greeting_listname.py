# write a prog to greet all the person na,es stored in a list and which starts with s

l = ["rutik", "sachin", "rahul", "sammer"]

for name in l:
    if name.startswith("s"):
        print("Hello " + name)
